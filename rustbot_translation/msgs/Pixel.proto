syntax = "proto3";

/*Although they are each channel color is uint8, we store them as uint32, see*/
/*https://groups.google.com/forum/#!topic/protobuf/Er39mNGnRWU*/

message Pixel {
    uint32 r = 1;
    uint32 g = 2;
    uint32 b = 3;
}

